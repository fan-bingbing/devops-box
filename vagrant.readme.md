## from a fresh ubuntu machine, following commands needs to be run before running C program
```bash
sudo apt-get update
sudo apt-get install build-essential
sudo apt-get install valgrind # debug software



